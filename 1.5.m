% 计算数列{x_n}的前10项
% 定义参数
m = 50;  % 这里假设m=50，您可以根据实际情况修改
n = 10;  % 计算前10项

% 初始化数组存储结果
x = zeros(1, n);
x(1) = rand(1);  % 第一项为0到1之间的随机数

% 使用循环计算后续项
for i = 2:n
    x(i) = sqrt(m/100 + x(i-1));
end

% 显示结果（以向量形式输出）
disp('数列的前10项为：');j
disp(x);
